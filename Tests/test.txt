intero soma(intero x, intero y) {
    intero a;
    a = x + y;
    retorna a;
}

intero fatorial(intero n){
    se (n == 0){
        retorna 1;
    }
    retorna n*fatorial(n-1);
}

intero main() {
    intero a;
    intero b;
    intero x;
    a = 3;
    x = 4;
    b = soma(a, 4);
    x = fatorial(x);
    printa(x);

    se (10==1 && 2==2 ) {
        printa("sim");
    } senao {
        printa("nao");
    }

    se ((1==1 && 2 < 4 ) || (3==1 && 3 > 4)) {
        printa("sim");
    }

    se (verdade && !falso) {
        printa("true");
    } senao {
        printa("falso");
    }


    intero z;
    z = 0;
    enquanto (z < 3) {
        z = z + 1;
        printa("while");
    }
}